import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    height: 500,
    padding: 20,
    paddingTop: 50,
    backgroundColor: 'black',
    alignItems: 'center',
  },
});
